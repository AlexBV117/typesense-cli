import Parser from "../src/Modules/Parser"

test("Test => Parser => Index => Normal", () => {
    let args = []
    let Parser = new Parser(args);
});

test("Test => Parser => Index => Append",() => {
    let args = []
    let Parser = new Parser(args);
});

test("Test => Parser => Index => Error", () => {
    let args = []
    let Parser = new Parser(args);
});

// test("Test the index token generation", () => {
//     let args = []
//     let Parser = new Parser(args);
// });

// test("Test the index token generation", () => {
//     let args = []
//     let Parser = new Parser(args);
// });

// test("Test the index token generation", () => {
//     let args = []
//     let Parser = new Parser(args);
// });

// test("Test the index token generation", () => {
//     let args = []
//     let Parser = new Parser(args);
// });